import org.junit.jupiter.api.Test;

public class ShoppingTest {

  @org.junit.jupiter.api.Test
  public void testShoppingSize() {
    //Sked sked = new Sked();
    //assert equals(1, sked.getName());
    //Sked sked2 = new Sked();
    //assert equals(2, sked2.getName());
    // Тест никак не запускался...

  }


}
